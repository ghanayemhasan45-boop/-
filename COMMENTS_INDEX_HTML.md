/**
 * =====================================================
 * شرح ملف HTML الرئيسي لمشروع ميرا جولد
 * =====================================================
 * * 1. الرأس (Head):
 * - Tailwind CSS: مكتبة التصميم الأساسية (استخدمناها بدل بوتستراب لمرونتها وسرعتها).
 * - Chart.js: مكتبة برمجية لرسم الإحصائيات والرسوم البيانية.
 * - خط Cairo: خط عربي مميز لمظهر عصري.
 * * 2. الهيدر (Header):
 * - لوجو "ميرا جولد" المصمم بالـ CSS (حرف M بخلفية متدرجة).
 * - زر "تحليلات" (Dashboard) لفتح الرسوم البيانية.
 * - أيقونة السلة (Shopping Cart) مع عداد للمنتجات (Badge) يظهر ويختفي ديناميكياً.
 * * 3. قسم الترحيب (Hero Section):
 * - خلفية داكنة مع تدرج لوني ذهبي (Gradient) لتعكس فخامة المجوهرات.
 * * 4. لوحة الإحصائيات (Dashboard):
 * - مخفية افتراضياً، وتظهر عند الضغط على زر "تحليلات".
 * - تحتوي على رسم بياني دائري (Doughnut) لتوزيع الفئات، ورسم عمودي (Bar) لمتوسط الأسعار.
 * * 5. منطقة الفلترة والمنتجات (Filters & Grid):
 * - أزرار الفلترة (خواتم، سلاسل، أطقم، أساور، أقراط).
 * - مربع بحث نصي سريع (Live Search).
 * - قائمة منسدلة لترتيب المنتجات (الأحدث، الأقل سعراً، الأعلى سعراً).
 * - شبكة المنتجات (Products Grid) المخفية في الـ HTML والتي يتم رسمها بالـ JavaScript.
 * * 6. السلة الجانبية (Cart Drawer):
 * - نافذة تفتح بانزلاق من الجنب (Drawer) لعرض المشتريات.
 * - زر "إرسال الطلب واتساب" المربوط برقم خدمة العملاء.
 */