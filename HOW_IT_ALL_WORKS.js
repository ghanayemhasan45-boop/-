/**
 * =====================================================
 * كيف يعمل موقع ميرا جولد؟ (Business Logic Flow)
 * =====================================================
 * * 1. عند فتح الموقع (Initialization):
 * - يبدأ ملف `main.js` بالعمل فور تحميل الصفحة.
 * - يستدعي دالة `renderFilters()` لإنشاء أزرار الأقسام (سلاسل، خواتم، إلخ).
 * - يستدعي دالة `renderGrid()` لقراءة مصفوفة المنتجات من `data.js` ورسمها في الصفحة.
 * * 2. عند البحث أو الفلترة (Filtering & Sorting):
 * - المستخدم يكتب في مربع البحث أو يختار قسم معين.
 * - يتم تحديث المتغيرات في كائن `state` (مثلاً: state.searchQuery).
 * - يتم استدعاء دالة `renderGrid()` مرة أخرى لتنقية (Filter) المنتجات ورسم الكروت المطابقة فقط.
 * * 3. إضافة منتج للسلة (Cart Operations):
 * - يضغط المستخدم على علامة "+" في كارت المنتج.
 * - تعمل دالة `addToCart(id)` الموجودة في ملف `cart.js`.
 * - يتم البحث عن المنتج برقم الـ ID وإضافته لمصفوفة `state.cart`.
 * - يتم استدعاء دالة `updateCartUI()` لتحديث إجمالي السعر وعداد المنتجات في الهيدر.
 * - تظهر رسالة خضراء (Toast) تفيد بنجاح الإضافة.
 * * 4. إرسال الطلب (Checkout):
 * - يضغط المستخدم على زر "إرسال الطلب واتساب".
 * - تقوم دالة `checkout()` بعمل Loop على مصفوفة السلة لتجميع الأسماء والكميات والأسعار.
 * - يتم إنشاء رابط واتساب، مع تشفير نص الرسالة برمجياً (encodeURIComponent).
 * - يفتح الموقع تطبيق الواتساب وبداخله الفاتورة جاهزة للإرسال.
 * * 5. عرض الإحصائيات (Analytics):
 * - عند الضغط على زر التحليلات، تظهر لوحة التحكم المخفية.
 * - تعمل دالة `initCharts()` لأول مرة فقط (لكي لا تستهلك موارد المتصفح).
 * - تقوم الدالة بحساب عدد المنتجات في كل فئة ومتوسط أسعارها، وتعطي البيانات لمكتبة Chart.js لرسمها.
 */